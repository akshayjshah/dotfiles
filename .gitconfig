[user]
	name = Akshay Shah
	email = shah@uber.com
[core]
	editor = emacsclient -t
	pager = less -FRSX
	excludesfile = ~/.gitignore_global
	autocrlf = input
[branch]
	autosetuprebase = always
[fetch]
	prune = true
[push]
	default = simple
[rerere]
	enabled = true
[color]
	UI = true
[diff]
	algorithm = patience
[format]
	pretty = format:%C(yellow)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset
	abbrev-commit = true
	date = relative
[alias]
	a = add
	amend = commit -a --amend
	c = commit
	co = checkout
	frb = !git fetch origin && git rebase origin/master
	graph = log --graph
	l = log origin/master..
	s = status
	save = !git add -A && git commit -m 'Savepoint'
	stashall = stash --include-untracked
	undo = reset --mixed HEAD^
	wip = !git add -u && git commit -m 'WIP'
	wipe = !git add -A && git commit -qm 'Wipe savepoint' && git reset --hard HEAD^
	wtf = log --follow --stat -p

	trim = !sh -c 'git branch -D $1 && git push origin :$1' -
	deforest = !sh -c 'git checkout master && git branch --merged | grep -v master | grep -v bugs | xargs -I "{}" git trim "{}" && git branch -v'
[github]
	user = akshayjshah
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
[url "ssh://gitolite@code.uber.internal:2222/"]
	insteadOf = https://code.uber.internal/